# daml-testing examples

Daml Script test modules referenced by `../SKILL.md`. Must run under Daml Script
with the pinned SDK.

Planned (to add during drafting):

- `BasicTest.daml` — allocate parties, create + exercise, read back.
- `AuthorizationTest.daml` — `submitMustFail` proving unauthorized parties cannot act.
