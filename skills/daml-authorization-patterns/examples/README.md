# daml-authorization-patterns examples

Compilable Daml samples referenced by `../SKILL.md`, demonstrating structural
authorization. Must build against the pinned Daml SDK.

Planned (to add during drafting):

- `ProposeAccept.daml` — initiate-and-accept to gather multi-party authority.
- `Delegation.daml` — a contract granting a specific right.
- `Roles.daml` — role-based access via separate templates.
